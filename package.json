{
  "name": "steam-mcp-server",
  "version": "0.4.0",
  "description": "MCP server for Steam Web API over HTTP (Cloudflare Worker)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "start": "wrangler dev",
    "dev": "tsc --watch",
    "dev:worker": "wrangler dev",
    "deploy:worker": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "version:patch": "npm version patch -m \"chore(release): %s\"",
    "version:minor": "npm version minor -m \"chore(release): %s\"",
    "version:major": "npm version major -m \"chore(release): %s\""
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "steam",
    "steam-api",
    "claude",
    "ai",
    "llm"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sharkusmanch/steam-mcp-server.git"
  },
  "bugs": {
    "url": "https://github.com/sharkusmanch/steam-mcp-server/issues"
  },
  "homepage": "https://github.com/sharkusmanch/steam-mcp-server#readme",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.0.15"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
